% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_delaunay.R
\name{link_delaunay}
\alias{link_delaunay}
\title{link_delaunay}
\usage{
link_delaunay(geog, max.dist = NULL, replace = FALSE, verbose = TRUE)
}
\arguments{
\item{geog}{\code{geoglist}. The output of \code{rast_to_geoglist()}.}

\item{max.dist}{\code{numeric} or \code{NULL}. If not \code{NULL}, then the maximum link
length permitted in metres. Only links below this threshold will be returned.}

\item{replace}{\code{logical}. Should any existing links in \code{geog} be replaced?
Defaults to \code{FALSE}, in which case links are appended to any existing ones.}

\item{verbose}{\code{logical}. Should function progress be to the user? This may
be useful when dealing with large rasters (high resolution and/or many
layers).}
}
\value{
The input \code{geoglist} with the \verb{$links} element filled with a
\code{SpatVector} of lines representing the island links, and recording their
start (\verb{$srt}) and end (\verb{$end}) cell IDs, the landscape layer to which they
belong (\verb{$layer}) and their lengths in metres (\verb{$distance}). If no landscape
layers contained islands, then \verb{$links} will remain \code{NULL}.
}
\description{
Calculate links between boundary cells between isolated regions within a
\code{geoglist} layer using Delaunay triangulation. Links can be discarded if they
fall above a threshold distance.
}
\details{
Linkage is a key feature of \code{rTARDIS} to permit movement otherwise
disconnected regions in geographic space. Delaunay triangulation is used to
calculate line-of-site linkage between cells at the edges of Voronoi-adjacent
islands (see \code{domains()}). Links which intersect unmasked regions other than
at their start and end points are discarded, as are those which start and
terminate on the same region. The result of this procedure is denser network
of links compared to those generated by \code{link_mask()}, where single links are
generated between Voronoi-adjacent islands.

Currently, links are calculated using \code{terra::delaunay()}. While \code{terra}
generally uses spherical geometry, this function appears to be a rare
exception where spherical geometry is not yet supported. As such, Delaunay
linkage on global scales is approximate, although it still appears to return
reasonable results.
}
\examples{
\donttest{
library(terra)
library(rTARDIS)

# load a dataset of the Galapagos archipelago through geological time
gal <- galapagos()

# create a land-sea mask from the archipelago raster set
gal_m <- classify(gal, matrix(c(-Inf, 0, NA, 0, Inf, 1), ncol = 3, byrow = TRUE), right = FALSE)

# create a geoglist with hexagonal resampling and mask the sea
hexes <- rast_to_geoglist(gal, gal_m, times = c(seq(2.25, 0, -0.5), 0), as.hex = TRUE, hex = 6)
hexes <- link_delaunay(hexes)

# plot the first layer of the geoglist with island links
plot(hexes)
}
}
