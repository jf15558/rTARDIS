% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_islands.R
\name{link_islands}
\alias{link_islands}
\title{link_islands}
\usage{
link_islands(geog, klink = NULL, replace = FALSE, verbose = TRUE)
}
\arguments{
\item{geog}{\code{geoglist}. The output of \code{rast_to_geoglist()}.}

\item{klink}{\code{integer} or \code{NULL}. The k-nearest Voronoi neighbours to be
linked for each island. Defaults to \code{NULL}, in which case the full Voronoi
neighbourhood of each island will be linked.}

\item{replace}{\code{logical}. Should any existing links in \code{geog} be replaced?
Defaults to \code{FALSE}, in which case links are appended to any existing ones.}

\item{verbose}{\code{logical}. Should function progress be to the user?}
}
\value{
The input \code{geoglist} with the \verb{$links} element filled with a
\code{SpatVector} of lines representing the island links, and recording their
start (\verb{$srt}) and end (\verb{$end}) cell IDs, the landscape layer to which they
belong (\verb{$layer}) and their lengths in metres (\verb{$distance}). If no landscape
layers contained islands, then \verb{$links} will remain \code{NULL}.
}
\description{
Calculate links between the nearest cells of isolated regions within a
\code{geoglist} layer, based on their Voronoi adjacency.
}
\details{
Linkage is a key feature of \code{rTARDIS} to permit movement otherwise
disconnected regions in geographic space. Linkage between islands is based on
their Voronoi neighbourhoods (see \code{domains()}). This can be thought of as
detecting line-of-site between all islands in a layer, then ranking those
line-of-site neighbours by the shortest distances required to reach them.
Links which intersect other islands or their Voronoi neighbourhoods are
considered invalid and discarded internally. The number of links between
neighbours is then determined by \code{klink}.

For \code{klink = 1}, the returned links will form a minimum spanning tree,
ensuring that every island can be reached by some route from any other
island. This is a conservative method of linkage and islands may instead have
many neighbours of similar distances. Setting \code{klink} to higher values will
therefore create an increasingly densely connected neighbourhood of islands.
\code{klink} can be set as high as the user likes, but the Voronoi neighbourhood
of an island will set the maximum number of links which the function will
return.

Currently, Voronoi neighbourhoods are calculated using \code{terra::voronoi()}.
While \code{terra} generally uses spherical geometry, this function appears to be
a rare exception where spherical geometry is not yet supported. As such,
Voronoi neighbourhoods on global scales are approximate, although they still
appear to return reasonable results.
}
\examples{
\donttest{
library(terra)
library(rTARDIS)

# load a dataset of the Galapagos archipelago through geological time
gal <- galapagos()

# create a land-sea mask from the archipelago raster set
gal_m <- classify(gal, matrix(c(-Inf, 0, NA, 0, Inf, 1), ncol = 3, byrow = TRUE), right = FALSE)

# create a geoglist with hexagonal resampling and mask the sea
hexes <- rast_to_geoglist(gal, gal_m, times = c(seq(2.25, 0, -0.5), 0), as.hex = TRUE, hex = 6)
hexes <- link_islands(hexes)

# plot geoglist with links
plot(hexes)
}
}
