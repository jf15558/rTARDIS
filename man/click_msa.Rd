% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/click_msa.R
\name{click_msa}
\alias{click_msa}
\title{click_msa}
\usage{
click_msa(
  tardis,
  weights = "gdist",
  geog,
  time = NULL,
  n = 1,
  col = "gold",
  ...
)
}
\arguments{
\item{tardis}{\code{tardis}. The output of \code{build_tardis()}.}

\item{weights}{\code{character}. The name of the weighting scheme in \code{tardis} to
use for distance calculation. By default these are true geographic distances
(\code{"gdist"}). Alternatively, the name of another weighting scheme added to
\code{tardis} using \code{weight_tardis()}.}

\item{geog}{\code{geoglist}. The \code{geoglist} used to build \code{tardis}.}

\item{time}{\code{integer}. The \code{tardis} time slice to plot and interact with.
Defaults to \code{NULL}, in which case the first slice is used}

\item{n}{\code{integer}. The number of points to link with a minimum spanning
arborescence.}

\item{col}{\code{character}. The colour to use for plotting the minimum spanning
arborescence.}

\item{...}{Additional arguments passed to \code{plot.geoglist()}}
}
\value{
No return value.
}
\description{
Interactively calculate and display a minimum spanning arboresence for a set
of points by clicking on the landscape plotted by the function. Clicked
points falling in masked regions are automatically resolved to the nearest
available cell.
}
\examples{
\donttest{
library(terra)
library(rTARDIS)

gal <- galapagos()
gal_m <- classify(gal, matrix(c(-Inf, 0, NA, 0, Inf, 1), ncol = 3, byrow = TRUE), right = FALSE)
hexes <- rast_to_geoglist(gal, gal_m, times = c(seq(2.25, 0, -0.5), 0), as.hex = TRUE, hex = 7)
hlink <- link_islands(hexes)
htd <- build_tardis(hexes)

# click a point on the map
#click_iso(tardis = htd, geog = hexes, time = 2, cost = 1e5)
}
}
