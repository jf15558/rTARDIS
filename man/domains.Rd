% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domains.R
\name{domains}
\alias{domains}
\title{domains}
\usage{
domains(geog)
}
\arguments{
\item{geog}{\code{geoglist}. The output of \code{rast_to_geoglist()}.}
}
\value{
A \code{SpatVectorCollection} with each element containing the Voronoi
polygons for the corresponding layer in the input \code{geoglist}.
}
\description{
Calculate Voronoi domains around islands in a \code{geoglist}. These polygonal
domains are the basis for how connections are determined across masked areas.
}
\examples{
\donttest{
library(terra)
library(rTARDIS)

# load a dataset of the Galapagos archipelago through geological time
gal <- galapagos()

# create a land-sea mask from the archipelago raster set
gal_m <- classify(gal, matrix(c(-Inf, 0, NA, 0, Inf, 1), ncol = 3, byrow = TRUE), right = FALSE)

# create a geoglist with hexagonal resampling and mask the sea
hexes <- rast_to_geoglist(gal, gal_m, times = c(seq(2.25, 0, -0.5), 0), as.hex = TRUE, hex = 6)

dms <- domains(hexes)
plot(hexes, 1)
plot(dms[[1]], add = TRUE)
}
}
